/* ARRAYS O ARREGLOS

un array es una estructura de datos que se utliza para almacenar una coleccion ordenada de elementos estos elementos pueden ser tambien un conjunto de funciones e inclusive otros arrays 
*/

let numeros = []; // declaracion de un array vacio

numeros = [15, 80, 650, 50, 43, -10];
console.log(numeros)// se improme por consola todo el array
console.log(numeros[4]);// se imprime por consola eñ 5to elemente que pertenece el indice 4.

